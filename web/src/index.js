//Importamos em todos arquivos JS que usaremos HTML
import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

//Renderizando/desenhando o conteúdo do <App /> dentro do 'root'
ReactDOM.render(<App />, document.getElementById('root'));
